name: air_quality_monitoring
description: "Air quality monitoring system (group 1 project 2)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.12
  firebase_auth: ^5.7.0
  intl: ^0.19.0
  google_maps_flutter: ^2.18.0
  geolocator: ^14.0.3
  flutter_local_notifications: ^18.0.1
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
